mail {
    host = "smtp.mail.ru"
    port = 465
    username = "mail@mail.ru"
    password = password
    useSSL = true
    useTLS = false
    debug = true
    fromName = "Name"
}

include required(file("${masterjava.config}/conf/mail.conf"))